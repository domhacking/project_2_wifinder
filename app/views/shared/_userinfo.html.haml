- if user_signed_in?
  %h1 
    User Info
    %p
      %b Image
      .circular
        = image_tag(current_user.profile_image.thumb.url)
      %p
        %b User Name:
        = current_user.name

      %p
        %b Email:
        = current_user.email
      %p
        %b Favourites: